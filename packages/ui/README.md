# `@studyhive/ui`

StudyHive's accessible design-system foundation. It owns the approved semantic tokens and the small utilities required by project-owned shadcn/ui components.

Sprint 0 intentionally includes no speculative component catalog. Components are added only when a documented screen or a proven shared foundation requires them, with accessibility tests and an intentional public export.
