# Containers

This directory owns the supported local Docker Compose profile and build definitions. The profile starts the web app, API, PostgreSQL, and Redis with safe local-only credentials from `.env`.

Deployment configuration is intentionally absent during Sprint 0. These images verify repeatable builds and local development; production topology arrives only in its approved roadmap phase.
